/**
 * Configuración de desarrollo (local).
 * Cuando ejecutas `npm start`, Angular usa este archivo.
 * En producción, file replacement de angular.json lo reemplaza por environment.prod.ts.
 */
export const environment = {
  production: false,
  serverUrl: 'http://localhost:3000'
};
